tempc = float(input("Digite uma temperatura em Celsius: "))

#não precisa de parentenses pq a ordem de procedencia é a ordem de operações
tempf = 9 * tempc / 5 +32

print(f'A temperatura de {tempc}°C corresponde a {tempf}°F!')